$(function(){
  // リストを空にする
  $('#stores').html('');

  // キーワードに応じて返ってくる店名と住所をリスト表示
  <% @descriptions.each do |description| %>
    $('#stores').append("<li class='col'><%= description %></li>");
  <% end %>
});